document.getElementById('recordBtn').addEventListener('click', startRecording);

function startRecording() {
    // Sprachaufnahme und -verarbeitung
    alert('Aufnahme gestartet (Demo)!');
    // Hier würde die Logik zur Sprachaufnahme implementiert werden.
}
